# Thinking

这些 skills 的核心目标不是让 LLM “多写”，而是让它在医学科研任务里更有秩序地判断、组织和执行。

## 1. 科学逻辑优先

科研写作先处理论证，再处理句子。一个 manuscript、abstract 或 slide deck 应该先明确：

- 研究的主问题是什么；
- 主要发现支持了什么判断；
- 最可能被 reviewer 质疑的地方在哪里；
- Methods、Results、Discussion 是否共同服务同一个主张。

这也是 `nature-masterclass-writing-skill` 和 `czf-writing-skill` 的基础：不能用漂亮语言掩盖松散逻辑。

## 2. 风格不是装饰，而是约束

`ajhg-polishing-skill` 的重点是克制、准确、概念先行。好的科研英文不需要夸张，也不需要过度修辞；它应该让贡献、证据和边界更清楚。

## 3. 工作流要可重复

复杂任务需要固定顺序：

1. 先判断科学结构。
2. 再润色表达。
3. 最后处理格式、文件和交付质量。

`czf-writing-skill` 把这个顺序显式化，避免 LLM 在写作、润色和排版之间来回跳。

## 4. 输出物要经过检查

无论是 Word 文稿还是 PPT，都不能只停在“生成完成”。需要检查：

- 结构是否完整；
- 图表和正文是否对应；
- 文本是否溢出；
- 格式是否符合目标场景；
- 是否引入了不存在的数据、引用或结论。

## 5. 共享知识应独立沉淀

多个 skill 会复用术语、翻译、版式和判断原则，因此共享内容放在 `skills/_shared/`。这样可以减少重复，也能让术语和风格保持一致。

